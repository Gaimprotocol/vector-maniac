import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.gaimstudios.galacticoverdrive',
  appName: 'Galactic Overdrive',
  webDir: 'dist'
};

export default config;
